import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sentinel.micp2psender',
  appName: 'P2P Mic Sender',
  webDir: 'dist'
};

export default config;
